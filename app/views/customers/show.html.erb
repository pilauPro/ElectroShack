<h1>Welcome, <%= @customer.first_name %></h1>
<h3>Order History</h3>
<% @customer.orders.each do |o| %>
  <h4>order id: <%= link_to o.id, order_path(o) %></h4>
  <% o.products.each do |p| %>
    <%= link_to p.name, product_path(p) %><br>
  <% end %>
  <br>
  <h4>order total: $<%= o.total %></h4>
<% end %>
